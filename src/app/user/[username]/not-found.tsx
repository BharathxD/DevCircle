"use client";

import NotFoundPage from "@/components/UI/NotFound";

const NotFound = () => {
  return (
    <NotFoundPage context={"User with the given username doesn't exist."} />
  );
};

export default NotFound;
